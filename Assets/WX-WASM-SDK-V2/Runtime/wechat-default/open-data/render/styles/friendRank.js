export default function getStyle(data) {
    return {
        // 
        container: {
            width: data.width,
            height: data.height,
            paddingLeft: 0,
            paddingRight: 0,
        },
        rankList: {
            width: data.width,
            height: data.height,
        },
        list: {
            width: data.width,
            height: data.height,
        },
        listItem: {
            position: 'relative',
            width: data.width,
            height: data.height / 10,
            flexDirection: 'row',
            alignItems: 'center',
            marginTop: 2,
        },
        rankCommonBg: {
            position: 'absolute',
            top: 0,
            left: 0,
            width: data.width,
            height: data.height / 10,
        },
        rankIndex: {
          position: 'absolute',
          top: (data.height / 10) * 0.35,
          left: data.width * 0.04,
          width: data.width * 0.1,
          height: (data.height / 10) * 0.3,
          lineHeight: (data.height / 10) * 0.3,
          textAlign: 'center',  
          fontSize: 30,
          color: '#fff',
        },
        rankAvatar: {
            position: 'absolute',
            top: (data.height / 10) * 0.2,
            left: data.width * 0.18,
            width: (data.height * 0.06),
            height: (data.height * 0.06),
        },
        rankName: {
            position: 'absolute',
            top: (data.height / 10) * 0.35,
            left: data.width * 0.3,
            width: data.width * 0.2525,
            height: (data.height / 10) * 0.3,
            lineHeight: (data.height / 10) * 0.3,
            textAlign: 'left',  
            fontSize: 30,
            textOverflow: 'ellipsis',
            color: '#fff',
        },
        rankScoreVal: {
            position: 'absolute',
            bottom: (data.height / 10) * 0.35,
            right: data.width * 0.114,
            width: data.width * 0.18,
            height: (data.height / 10) * 0.3,
            lineHeight: (data.height / 10) * 0.3,
            textAlign: 'right',
            fontSize: 30,
            color: '#fff',
        },
        rankScoreTip: {
            position: 'absolute',
            bottom: (data.height / 10) * 0.35,
            right: data.width * 0.075,
            width: data.width * 0.15,
            height: (data.height / 10) * 0.3,
            lineHeight: (data.height / 10) * 0.3,
            textAlign: 'right',  
            fontSize: 30,
            color: '#fff',
        },
    };
}
